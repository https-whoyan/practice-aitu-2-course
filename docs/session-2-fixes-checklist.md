# Чек-лист исправлений Сессии 2

Прогресс по `session-2-fixes-plan.md`. Статусы: ⬜ не начато · 🟡 в работе · ✅ готово.

DoD на задачу: фикс на корневом слое + проверка (тест правил/ручной сценарий) +
`flutter analyze` чисто (ASCII-путь) + `npm run build` в `functions/`.

## Этап 0 — Безопасность (P0)

| # | Задача | Слой | Статус |
|---|--------|------|--------|
| P0-1 | Лок полей попытки квиза (`answers`/`status`/`finishedAt` only) | Rules | ✅ |
| P0-2 | Дедлайн/лимит времени квиза на submit + таймер | CF + UI | ✅ |
| P0-3 | Enforce дедлайна задания | Rules | ✅ |
| P0-4 | Преподаватель читает grades/submissions только своих курсов | Rules | ✅ |
| P0-5 | Студент не читает неопубликованное | Rules | ✅ |
| P0-6 | Анти-перебор кодов приглашения | CF | ✅ |
| P0-7 | Storage: чтение файлов сдач только своего курса | Storage Rules | ✅ |
| P0-тесты | deny/allow на каждый P0 (31/31 зелёные) | tests | ✅ |

**Проверки P0:** Security Rules — 31/31 тест зелёный (эмулятор); `npm run build`
в `functions/` — чисто; `flutter analyze` — чисто (на ASCII-копии, баг кириллицы
в пути); `storage.rules` парсится эмулятором.

**Открытый вопрос P0-6:** App Check намеренно НЕ включён (без регистрации
клиентов он отклонит все вызовы CF). Анти-перебор закрыт rate-limit'ом +
8-значным кодом. App Check — отдельной задачей при настройке App Check на клиенте.

## Этап 1 — Подключение бэкенда (P1)

| # | Задача | Слой | Статус |
|---|--------|------|--------|
| P1-1 | Связь группа↔курс + студенты (форма группы админа) | UI | ✅ |
| P1-2 | Создание кода приглашения преподавателем | UI + CF | ✅ |
| P1-3 | «Мои группы» преподавателя (список + детали) | UI + provider | ✅ |
| P1-4 | Действия над вопросом (duplicate/archive/delete/attach) | UI | ✅ |
| P1-5 | Фильтры банка вопросов (QuestionFilter) | UI | ✅ |
| P1-6 | Ручные оценки + фильтры журнала | UI + provider | ✅ |
| P1-7 | История изменения оценки (раскрытие) | UI | ✅ |
| P1-8 | Reveal ответов квиза + история попыток | UI | ✅ |
| P1-9 | Edit-профиль + фото; поля статус/даты/группы/курсы | UI + Storage | ✅ |
| P1-10 | Email-верификация: гейт в роутере + CF подтверждения | Router + CF | ✅ |

**Проверки P1:** `flutter analyze lib` — чисто (2 предсуществующих `info` в
`auth/domain` не трогали); `npm run build` в `functions/` — чисто; codegen
(build_runner) прогнан для новых провайдеров.

**Новое (P1):**
- Провайдеры: `teacherGroupsByCourses`, `courseGroups`, `courseStudents`.
- Метод репозитория: `GroupRepository.watchGroupsForCourses`.
- CF: `confirmEmailVerified` (подтверждение email → `status: active`).
- Экраны: `TeacherGroupsScreen`, `TeacherGroupDetailScreen`, `EditProfileScreen`.
- Сервис: `AvatarStorageService` (аватары в Storage).

**На подтверждение (P1-10):** гейт верификации применяется ко всем
`pendingVerification`, включая админ-созданных пользователей — они тоже
пройдут верификацию email при первом входе. Если для админ-созданных аккаунтов
нужно авто-active, скажи — вынесу в отдельную ветку логики.

## Этап 2 — Корректность данных (P2)

| # | Задача | Статус |
|---|--------|--------|
| P2-1 | Соавторы-преподаватели курса (CF add-by-email + remove) | ✅ |
| P2-2 | Назначение курса группам (админ-форма) | ✅ |
| P2-3 | Удаление курса (`deleteCourse` + защита) | ✅ |
| P2-4 | Даты начала/завершения курса | ✅ |
| P2-5 | Загрузка обложки курса в Storage | ✅ |
| P2-6 | Перевод студента между группами | ✅ |
| P2-7 | Сброс пароля (`resetUserPassword` CF) | ✅ |
| P2-8 | Правка имени пользователя админом | ✅ |
| P2-9 | Штраф за просрочку (`latePenalty`) | ✅ |
| P2-10 | Статусы reject/inReview | ✅ |
| P2-11 | Фильтры/сортировка отправок + имя студента | ✅ |
| P2-12 | groupId оценок за задание | ✅ |
| P2-13 | Балл студенту на странице задания | ✅ |
| P2-14 | Страница курса студента: преподаватель/даты/прогресс | ✅ |
| P2-15 | Экспорт журнала в CSV-файл | ✅ |
| P2-16 | Импорт: дедупликация (инлайн-правка полей — отложено) | 🟡 |
| P2-17 | Ручной выбор вопросов в квиз + maxScore | ✅ |
| P2-18 | Экран результатов квиза (преподаватель) | ✅ |
| P2-19 | Фильтр пользователей по статусу (группа/курс — отложено) | 🟡 |
| P2-20 | Уведомления: типы + адресация студентам курса | ✅ |
| P2-21 | Назначение преподавателей на курс (админ) | ✅ |
| P2-22 | Поиск курсов студента (материалы/студенты — частично) | 🟡 |

**Проверки P2:** `flutter analyze lib` — чисто; `npm run build` в `functions/` —
чисто; codegen прогнан; Security Rules 31/31; storage+firestore rules парсятся.

**Новое (P2):** CF `addCourseTeacherByEmail`, `resetUserPassword`,
`onWeekNotify`/`onQuizNotify`/`onSubmissionNotify`; модель Course (`startDate`/
`endDate`), Assignment (`latePenalty`), статус `rejected`; `deleteCourse`,
`watchGroupsForCourses`/`courseGroups`/`courseStudents`,
`watchAllAttempts`/`quizAllAttempts`; сервисы `CourseCoverService`; storage-
правило `covers/`; экран `QuizResultsScreen`.

**Отложено (3 хвоста):** инлайн-правка полей при импорте (P2-16 — дедуп
сделан), фильтры пользователей по группе/курсу (P2-19 — статус сделан), поиск
по материалам и студентам преподавателя (P2-22 — курсы студента сделаны).

## Этап 3 — Заглушки → данные (P3)

| # | Задача | Статус |
|---|--------|--------|
| P3-1 | Dashboard'ы всех ролей (реальные данные) | ✅ |
| P3-2 | Статистика группы (`recomputeGroupSummary` + экран) | ✅ |
| P3-3 | Статистика курса (`recomputeCourseSummary` + экран) | ✅ |
| P3-4 | Мониторинг студентов (раскрытие в статистике) | ✅ |
| P3-5 | Статистика студента: фикс просрочек + квиз-балл/дедлайны | ✅ |
| P3-6 | Реэкспорт recompute* как callable CF | ✅ |
| P3-7 | Системная статистика админа (дашборд) | ✅ |
| P3-8 | systemSettings: типы вопросов/политики + журнал ошибок | ⬜ (отложено) |

**Проверки P3:** `flutter analyze lib` — чисто; `npm run build` в `functions/` —
чисто; codegen прогнан; Security Rules 31/31.

**Новое (P3):** CF `recomputeStudentStats`/`recomputeGroupStats`/
`recomputeCourseStats`; `recomputeGroupSummary`/`recomputeCourseSummary`;
исправлена метрика просрочек (`deadline<now && !submitted`); поля
`overdueCount`/`upcomingCount`/`averageQuizPercentage` в сводке студента;
коллекции `groupSummaries`/`courseSummaries` + правила; модели `GroupSummary`/
`CourseSummary`; экран `GroupStatsScreen` (курсы/группы/мониторинг);
dashboard'ы всех ролей с данными.

**Отложено:** P3-8 (новая фича: системные настройки типов вопросов/категорий/
политик + журнал ошибок) — согласовать объём с заказчиком.

## Этап 4 — Объём MVP→ТЗ (P4)

Большие пункты (типы вопросов 4→12, категории/рандомайзер, экспорт TXT/JSON/CSV,
связанная копия вопроса, in-app YouTube, **FCM push**, **offline**) — требуют
согласования MVP-среза с заказчиком, **не трогались**.

Сделано из P4 (безопасно, без согласования):
- ✅ **Индексы (§10)** — добавлены в `firestore.indexes.json`:
  `notifications(userId+createdAt desc)`, `notifications(userId+isRead)`,
  `users(role+email)` (под реальные запросы списков/уведомлений).

## Этап 5 — Полировка (P5)

| Пункт | Статус |
|-------|--------|
| Дедлайн задания — дата **+ время** | ✅ |
| Дедлайн/старт квиза — дата **+ время** | ✅ |
| Pre-start инфо квиза (вопросы/лимит/попытки/дедлайн до старта) | ✅ |
| `sourceId` оценки за задание = `assignmentId` (был `submissionId`) | ✅ |
| Аудит оценок: роль `system` для авто-оценок (был жёстко `teacher`) | ✅ |
| Поиск по материалам недели (хвост P2-22) | ✅ |
| Реордер материалов (`orderIndex`), 8-й тип материала, экран настроек | ⬜ |

**Проверки P4-инд/P5:** `flutter analyze lib` — чисто; `npm run build` — чисто;
`firestore.indexes.json` — валиден (14 индексов).
